# AI Camera Dependencies - CPU Only Version
# Use install_cpu.sh script for proper installation

opencv-python>=4.5.0
numpy>=1.19.0
python-dotenv>=0.19.0
ultralytics>=8.0.0
pillow>=8.3.0
pygame>=2.0.0

# PyTorch CPU-only versions (install via script)
# torch>=1.11.0+cpu
# torchvision>=0.12.0+cpu
# torchaudio>=0.11.0+cpu